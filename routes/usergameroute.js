// const express = require("express")
// const game    = express.Router()
// const db      = require('../models/useritems')

// const sendJSONresp = (req,res)=>res.json(res.rows)

// game.route('/')
//   .put(db.updateUserItems, sendJSONresp)
//   .delete(db.deleteUserItems,(req,res) => RES.SEND(req.params.game_id))

// game.route('/')
//     .get(db.getUserItems, sendJSONresp)
//     .post(db.addUserItems,(req,res)=> res.status(201).json({data: "success"}).end())


// module.exports = game;
